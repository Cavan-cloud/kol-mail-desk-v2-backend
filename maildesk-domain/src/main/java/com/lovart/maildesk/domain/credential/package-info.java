/**
 * External integration credentials (Google OAuth tokens, Feishu app secrets,
 * Kimi API keys). Persisted AES-256 encrypted in {@code integration_credentials}.
 */
package com.lovart.maildesk.domain.credential;
