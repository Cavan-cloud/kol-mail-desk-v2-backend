package com.lovart.maildesk.ai;

/**
 * AI pipeline entry points. Each maps to a row in {@code maildesk.ai.capabilities.*}.
 */
public enum AiCapability {
    CLASSIFY,
    DRAFT,
    CHECK,
    TRANSLATE
}
