/**
 * Business enumerations. Values mirror the corresponding PostgreSQL ENUM types
 * (see {@code V2__init_enums.sql}) — the {@link com.lovart.maildesk.common.typehandler.PgEnumTypeHandler}
 * translates between the Java {@code UPPER_CASE} constant and the database
 * {@code lower_case} label.
 */
package com.lovart.maildesk.common.enums;
